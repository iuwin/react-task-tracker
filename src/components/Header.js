import Button from './Button';

const Header = () => {
  return (
    <header className='header'>
        <h1>Tasks Tracker</h1>
        <Button color="blue" text="Add" />
    </header>
  )
}

export default Header